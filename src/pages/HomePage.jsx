import React from 'react';
import Loader from "../components/UI/Loader/Loader";

const HomePage = () => {
    return (
        <div>
            <Loader/>
        </div>
    );
};

export default HomePage;